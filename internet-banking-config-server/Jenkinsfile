pipeline {
    agent { label 'Node1' }

    environment {
        JAVA_HOME    = "/usr/lib/jvm/java-21-openjdk-amd64"
        PATH         = "${JAVA_HOME}/bin:${env.PATH}"
        DOCKER_USER  = "sudhakar20000"
        SERVICE_DIR  = "internet-banking-config-server"
        SERVICE_NAME = "config-server"
        IMAGE_TAG    = "${BUILD_NUMBER}"
    }

    stages {

        stage('Checkout') {
            steps {
                checkout scm
                echo "Building: config-server | Branch: ${env.BRANCH_NAME} | Tag: ${IMAGE_TAG}"
            }
        }

        stage('Build & Test') {
            steps {
                dir("internet-banking-config-server") {
                    sh 'chmod +x gradlew'
                    sh './gradlew clean build --no-daemon'
                }
            }
            post {
                always {
                    junit allowEmptyResults: true,
                          testResults: "internet-banking-config-server/build/test-results/**/*.xml"
                }
            }
        }

        stage('SonarQube Analysis') {
            steps {
                withSonarQubeEnv('sonarqube-scanner') {
                    script {
                        def scannerHome = tool 'sonarqube-scanner'
                        dir("internet-banking-config-server") {
                            sh "${scannerHome}/bin/sonar-scanner -Dsonar.projectKey=ib-config-server -Dsonar.projectName=ib-config-server -Dsonar.sources=src/main -Dsonar.tests=src/test -Dsonar.java.binaries=build/classes/java/main -Dsonar.exclusions=**/build/**"
                        }
                    }
                }
            }
        }

        stage('Quality Gate') {
            steps {
                timeout(time: 5, unit: 'MINUTES') {
                    waitForQualityGate abortPipeline: true
                }
            }
        }

        stage('Docker Build & Push') {
            steps {
                script {
                    dir("internet-banking-config-server") {
                        docker.withRegistry('https://index.docker.io/v1/', 'Dockerhub') {
                            def img = docker.build("sudhakar20000/ib-config-server:${BUILD_NUMBER}", ".")
                            img.push()
                            img.push('latest')
                        }
                    }
                }
            }
        }

        stage('Deploy to Kubernetes') {
            steps {
                withCredentials([file(credentialsId: 'kubeconfig', variable: 'KUBECONFIG')]) {
                    sh """
                        kubectl set image deployment/config-server \
                            config-server=sudhakar20000/ib-config-server:${BUILD_NUMBER} \
                            -n banking --kubeconfig=\$KUBECONFIG

                        kubectl rollout status deployment/config-server \
                            -n banking --timeout=300s --kubeconfig=\$KUBECONFIG
                    """
                }
            }
        }

        stage('Cleanup') {
            steps {
                sh "docker rmi sudhakar20000/ib-config-server:${BUILD_NUMBER} || true"
                sh "docker rmi sudhakar20000/ib-config-server:latest || true"
                cleanWs()
            }
        }
    }

    post {
        success { echo "✅ config-server pipeline SUCCESS" }
        failure { echo "❌ config-server pipeline FAILED" }
    }
}
